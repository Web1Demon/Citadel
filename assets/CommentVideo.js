import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const CommentVideo = ({item}) => {
  return (
    <View>
      <Text style={{color:'red'}}>CommentVideo</Text>
    </View>
  )
}

export default CommentVideo

const styles = StyleSheet.create({})